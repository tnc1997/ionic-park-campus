import {Home} from './home/home';
import {Setup} from './setup/setup';

/**
 * This is the page that the user will see for the first time after they have installed the application.
 *
 * @type {@link Setup}
 */
export const InstallationPage = Setup;

/**
 * This is the page that the user will see on all subsequent launches of the application.
 *
 * @type {@link Home}
 */
export const MainPage = Home;
